<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.mju.capston.whatisthisdog">
    <!-- 카메라 권한 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.BaseActivity"></activity>
        <activity android:name=".Activity.DogInfoActivity" />
        <activity android:name=".Activity.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.CategoryActivity" />
        <activity android:name=".Activity.AlbumActivity" />
        <activity android:name=".Activity.DictActivity" />
        <activity android:name=".Activity.MatchingActivity" />
        <activity android:name=".Activity.CameraActivity" />
        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>